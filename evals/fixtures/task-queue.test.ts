// Synthetic benchmark fixture. Generated by evals/fixtures/generate.sh.
// Not real production code; exists only to exercise bulk-read delegation.

import { describe, it, expect } from 'vitest';
import { TaskQueue } from './taskqueue';
import { FakeHttpClient } from './test-helpers/fake-http-client';
import { NullLogger } from './test-helpers/null-logger';

describe('TaskQueue', () => {

  it('fetchUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.fetchUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('createUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.createUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('updateUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.updateUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('deleteUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.deleteUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('listUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.listUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('syncUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.syncUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('validateUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.validateUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('parseUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.parseUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('serializeUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.serializeUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('deserializeUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.deserializeUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('connectUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.connectUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('disconnectUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.disconnectUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('retryUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.retryUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('cacheUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.cacheUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('invalidateUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.invalidateUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('refreshUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.refreshUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('publishUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.publishUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('subscribeUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.subscribeUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('archiveUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.archiveUser('1');
    expect(result).toEqual({ id: '1' });
  });

  it('restoreUser returns the User from the http client', async () => {
    const http = new FakeHttpClient({ ok: true, data: { id: '1' } });
    const taskqueue = new TaskQueue(http, new NullLogger());
    const result = await taskqueue.restoreUser('1');
    expect(result).toEqual({ id: '1' });
  });
});
