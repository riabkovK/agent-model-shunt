// Synthetic benchmark fixture. Generated by evals/fixtures/generate.sh.
// Not real production code; exists only to exercise bulk-read delegation.

import { ApiError } from './errors';
import type { HttpClient } from './http-client';
import type { Logger } from './logger';

export class ApiClient {
  constructor(private readonly http: HttpClient, private readonly logger: Logger) {}

  /**
   * fetchs a User by id. Synthetic benchmark fixture method.
   */
  async fetchUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.fetchUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`fetchUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * creates a User by id. Synthetic benchmark fixture method.
   */
  async createUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.createUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`createUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * updates a User by id. Synthetic benchmark fixture method.
   */
  async updateUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.updateUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`updateUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * deletes a User by id. Synthetic benchmark fixture method.
   */
  async deleteUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.deleteUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`deleteUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * lists a User by id. Synthetic benchmark fixture method.
   */
  async listUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.listUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`listUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * syncs a User by id. Synthetic benchmark fixture method.
   */
  async syncUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.syncUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`syncUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * validates a User by id. Synthetic benchmark fixture method.
   */
  async validateUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.validateUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`validateUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * parses a User by id. Synthetic benchmark fixture method.
   */
  async parseUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.parseUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`parseUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * serializes a User by id. Synthetic benchmark fixture method.
   */
  async serializeUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.serializeUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`serializeUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * deserializes a User by id. Synthetic benchmark fixture method.
   */
  async deserializeUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.deserializeUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`deserializeUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * connects a User by id. Synthetic benchmark fixture method.
   */
  async connectUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.connectUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`connectUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * disconnects a User by id. Synthetic benchmark fixture method.
   */
  async disconnectUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.disconnectUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`disconnectUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * retrys a User by id. Synthetic benchmark fixture method.
   */
  async retryUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.retryUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`retryUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * caches a User by id. Synthetic benchmark fixture method.
   */
  async cacheUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.cacheUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`cacheUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * invalidates a User by id. Synthetic benchmark fixture method.
   */
  async invalidateUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.invalidateUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`invalidateUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * refreshs a User by id. Synthetic benchmark fixture method.
   */
  async refreshUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.refreshUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`refreshUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * publishs a User by id. Synthetic benchmark fixture method.
   */
  async publishUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.publishUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`publishUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * subscribes a User by id. Synthetic benchmark fixture method.
   */
  async subscribeUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.subscribeUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`subscribeUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * archives a User by id. Synthetic benchmark fixture method.
   */
  async archiveUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.archiveUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`archiveUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * restores a User by id. Synthetic benchmark fixture method.
   */
  async restoreUser(id: string): Promise<User> {
    this.logger.debug(`ApiClient.restoreUser called with id=${id}`);
    const response = await this.http.request('POST', `/users/${id}`);
    if (!response.ok) {
      throw new ApiError(`restoreUser failed: ${response.status}`);
    }
    return response.data as User;
  }

  /**
   * fetchs a Order by id. Synthetic benchmark fixture method.
   */
  async fetchOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.fetchOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`fetchOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * creates a Order by id. Synthetic benchmark fixture method.
   */
  async createOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.createOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`createOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * updates a Order by id. Synthetic benchmark fixture method.
   */
  async updateOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.updateOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`updateOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * deletes a Order by id. Synthetic benchmark fixture method.
   */
  async deleteOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.deleteOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`deleteOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * lists a Order by id. Synthetic benchmark fixture method.
   */
  async listOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.listOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`listOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * syncs a Order by id. Synthetic benchmark fixture method.
   */
  async syncOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.syncOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`syncOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * validates a Order by id. Synthetic benchmark fixture method.
   */
  async validateOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.validateOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`validateOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * parses a Order by id. Synthetic benchmark fixture method.
   */
  async parseOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.parseOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`parseOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * serializes a Order by id. Synthetic benchmark fixture method.
   */
  async serializeOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.serializeOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`serializeOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * deserializes a Order by id. Synthetic benchmark fixture method.
   */
  async deserializeOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.deserializeOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`deserializeOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * connects a Order by id. Synthetic benchmark fixture method.
   */
  async connectOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.connectOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`connectOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * disconnects a Order by id. Synthetic benchmark fixture method.
   */
  async disconnectOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.disconnectOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`disconnectOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * retrys a Order by id. Synthetic benchmark fixture method.
   */
  async retryOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.retryOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`retryOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * caches a Order by id. Synthetic benchmark fixture method.
   */
  async cacheOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.cacheOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`cacheOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * invalidates a Order by id. Synthetic benchmark fixture method.
   */
  async invalidateOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.invalidateOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`invalidateOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * refreshs a Order by id. Synthetic benchmark fixture method.
   */
  async refreshOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.refreshOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`refreshOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * publishs a Order by id. Synthetic benchmark fixture method.
   */
  async publishOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.publishOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`publishOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * subscribes a Order by id. Synthetic benchmark fixture method.
   */
  async subscribeOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.subscribeOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`subscribeOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * archives a Order by id. Synthetic benchmark fixture method.
   */
  async archiveOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.archiveOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`archiveOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * restores a Order by id. Synthetic benchmark fixture method.
   */
  async restoreOrder(id: string): Promise<Order> {
    this.logger.debug(`ApiClient.restoreOrder called with id=${id}`);
    const response = await this.http.request('POST', `/orders/${id}`);
    if (!response.ok) {
      throw new ApiError(`restoreOrder failed: ${response.status}`);
    }
    return response.data as Order;
  }

  /**
   * fetchs a Product by id. Synthetic benchmark fixture method.
   */
  async fetchProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.fetchProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`fetchProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * creates a Product by id. Synthetic benchmark fixture method.
   */
  async createProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.createProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`createProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * updates a Product by id. Synthetic benchmark fixture method.
   */
  async updateProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.updateProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`updateProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * deletes a Product by id. Synthetic benchmark fixture method.
   */
  async deleteProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.deleteProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`deleteProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * lists a Product by id. Synthetic benchmark fixture method.
   */
  async listProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.listProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`listProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * syncs a Product by id. Synthetic benchmark fixture method.
   */
  async syncProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.syncProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`syncProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * validates a Product by id. Synthetic benchmark fixture method.
   */
  async validateProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.validateProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`validateProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * parses a Product by id. Synthetic benchmark fixture method.
   */
  async parseProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.parseProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`parseProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * serializes a Product by id. Synthetic benchmark fixture method.
   */
  async serializeProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.serializeProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`serializeProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * deserializes a Product by id. Synthetic benchmark fixture method.
   */
  async deserializeProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.deserializeProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`deserializeProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * connects a Product by id. Synthetic benchmark fixture method.
   */
  async connectProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.connectProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`connectProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * disconnects a Product by id. Synthetic benchmark fixture method.
   */
  async disconnectProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.disconnectProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`disconnectProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * retrys a Product by id. Synthetic benchmark fixture method.
   */
  async retryProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.retryProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`retryProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * caches a Product by id. Synthetic benchmark fixture method.
   */
  async cacheProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.cacheProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`cacheProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }

  /**
   * invalidates a Product by id. Synthetic benchmark fixture method.
   */
  async invalidateProduct(id: string): Promise<Product> {
    this.logger.debug(`ApiClient.invalidateProduct called with id=${id}`);
    const response = await this.http.request('POST', `/products/${id}`);
    if (!response.ok) {
      throw new ApiError(`invalidateProduct failed: ${response.status}`);
    }
    return response.data as Product;
  }
}
